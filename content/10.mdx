### Examples

<table>
<tr>
<th>React</th>
<th>React Native</th>
</tr>

<tr>
    <td>

    ```javascript

      import React from "react";

      const App = () => {
        return (
          <div
            style={{
              display: "flex",
              flexDirection: "column",
              alignItems: "center",
              justifyContent: "center"
            }}
            className="m-4 p-4 container"
          >
            Hello World
          </div>
        );
      };

      export default App;


    ```

    </td>
    <td>

    ```javascript

      import React from "react";
      import { StyleSheet, Text, View } from "react-native";

      const App = () => {
        return (
          <View styles={styles.container}>
            <Text
              style={{
                margin: 4,
                padding: 4,
              }}
            >
              App
            </Text>
          </View>
        );
      };

      export default App;

      const styles = StyleSheet.create({
        container: {
          alignItems: "center",
          justifyContent: "center",
        },
      });


    ```

    </td>

  </tr>
</table>
