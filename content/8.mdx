## What is Bridge?

- Bridge is the way in which JS communicates with native code.
- The bridge handles the serialization and deserialization of data between JavaScript and native code.
- Asynchronous communication as JS and Native runs on diffrent threads.

---

It is being replaced by JSI (JavaScript Interface) in newer react native versions
